<?php
// Database connection – credentials stored in plaintext (intentional finding)
define('DB_HOST', '{{ banking_db_host }}');
define('DB_USER', '{{ banking_db_user }}');
define('DB_PASS', '{{ banking_db_password }}');
define('DB_NAME', '{{ banking_db_name }}');

$conn = new mysqli(DB_HOST, DB_USER, DB_PASS, DB_NAME);
if ($conn->connect_error) {
    die("Connection failed: " . $conn->connect_error);
}
