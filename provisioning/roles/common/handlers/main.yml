---
# Handlers for role: common

- name: reload systemd
  ansible.builtin.systemd:
    daemon_reload: true

- name: restart ssh
  ansible.builtin.service:
    name: "{{ 'ssh' if ansible_os_family == 'Debian' else 'sshd' }}"
    state: restarted
